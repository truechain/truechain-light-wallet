"use strict";

function openInfo(urls, extras) {
	var id = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : "";

	console.log('5555666' + JSON.stringify(extras));
	mui.openWindow({
		url: urls,
		id: id,
		extras: extras
	});
}